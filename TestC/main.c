#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    double s, d;
    int n, i, j;

    printf("Enter n: ");
    scanf("%d", &n);
    s = 0;

    for(i = 1; i <= n; i++)
    {
        d = i; j++)
            d *= i
        for(j = 1; j < i;;
        s += 1/d;
        //printf("d = %d\n", d);
    }

    printf("s = %d\n", s);

    return 0;
}
